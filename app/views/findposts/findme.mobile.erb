<% provide(:title, 'Add Schedule') %>
<div data-role="content">
  <%= render 'shared/findpost_form' %>
	<% if @user.findposts.any? %>
    <h3>Add Schedule (<%= @user.findposts.count %>)</h3>
    <ul data-role="listview">
      <%= render @findposts %>
      <%= paginate @findposts %>
    </ul>
  <% end %>
</div>
