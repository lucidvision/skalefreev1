<% provide(:title, 'Wall') %>
<div class="row">
  <aside class="span4">
    <% if signed_in? %> 
      <section>
        <%= render 'shared/user_info' %>
      </section>
    <% else %>
      <section>
        <h1>Welcome to the Skalefree Wall!</h1> 
        Visit our <%= link_to "wall help", wall_help_path %> page for more information.<br><br>
        Please <%= link_to "sign up", :new_user_registration%> to create wallposts, access profiles, and message users.<br><br>
        Or search for walls below.<br><br>
      <section>
    <% end %>
    <section>
      <%= render 'shared/wall_form' %>
      <h4>You have selected the wall:<br><br><div id="wall_selection"></div></h4>
      <% if signed_in? %> 
        <%= render 'shared/forumpost_form' %>
      <% end %>
    </section>
  </aside>
  <div class="span8">
    <h3 class="f_title">Wallposts</h3>
    <ul class="wall_results">
      <%= render @wall %>
    </ul>
  </div>
  <div id="paginator">
    <%= paginate @wall, :theme => 'twitter-bootstrap', :remote => true %>
  </div>
</div>  
