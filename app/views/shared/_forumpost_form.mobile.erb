<%= form_for @forumpost, :remote => true do |f| %>
  <div class="field">
    <%= f.hidden_field :forum, :id => "wallpost" %>
    <%= f.label :content, "Write a Wallpost" %>
    <%= f.text_area :content %>
  </div>
  <%= f.submit "Create", :onclick => "refresh_search()", :id => "first" %>
<% end %>